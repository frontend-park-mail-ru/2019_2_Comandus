<div id="{{_id}}">
	<div class="tp-container tp-centered-container">
		<div class="tp-row">
			<div class="tp-col-8 ">
				<div class="card card_no-padding">
					{{#each jobs}}
						{{{this}}}
					{{/each}}
				</div>

			</div>
		</div>
	</div>
</div>
