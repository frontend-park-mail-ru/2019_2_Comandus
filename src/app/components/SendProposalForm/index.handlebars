<div id="{{_id}}" class="send-proposal-form">
	<form class="" method="post" id="addProposal" autocomplete="off">
		<div class="">
			{{{budgetField}}}
			{{{timeSelect}}}
			{{{proposalField}}}
		</div>
		<div class="job-details__inner-item">
			<div class="buttons-group">
				{{{submitProposal}}}
				{{{cancelBtn}}}
			</div>
		</div>
	</form>
</div>
